@model Factory.Models.Engineer

@{
    ViewData["Title"] = "Engineer Details";
}

<div class="container mt-5">
    <div class="row">
        <div class="col-md-6 offset-md-3">
            <div class="card">
                <div class="card-header">
                    <h3 class="card-title">@Model.Name</h3>
                </div>
                <div class="card-body">
                    <p><strong>Email:</strong> @Model.Email</p>
                    <p><strong>Phone:</strong> @Model.Phone</p>
                    <p><strong>License ID:</strong> @Model.LicenseID</p>
                    @if (Model.Machines.Count > 0)
                    {
                        <p><strong>Machines:</strong></p>
                        <table class="table table-bordered">
                            <thead>
                                <tr>
                                    <th>Name</th>
                                    <th>Description</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var machine in Model.Machines)
                                {
                                    <tr>
                                        <td>@machine.Name</td>
                                        <td>@machine.Description</td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    }
                    else
                    {
                        <p>No machines licensed for this engineer.</p>
                    }
                </div>
                <div class="card-footer text-center">
                    <a asp-action="Edit" asp-route-id="@Model.EngineerID" class="btn btn-primary mr-2">Edit</a>
                    <a asp-action="Delete" asp-route-id="@Model.EngineerID" class="btn btn-danger">Delete</a>
                </div>
            </div>
        </div>
    </div>
</div>
